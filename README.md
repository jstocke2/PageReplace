# PageReplace

A C++ operating-systems exercise that reads a page-request sequence from \`requests.txt\` and simulates page replacement.

## Build and run

\`\`\`sh
g++ pageReplace.cpp -o page-replace
./page-replace
\`\`\`

Edit \`requests.txt\` to try a different reference sequence.
